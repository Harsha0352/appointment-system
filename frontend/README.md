# Appointment Frontend

Simple React frontend for the appointment backend. Uses Vite.

Run locally:

```bash
cd frontend
npm install
npm run dev
```

By default the frontend calls `http://127.0.0.1:8000` for API endpoints. To change, set `VITE_API_BASE` in an `.env` file in `frontend/`.
